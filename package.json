{
  "name": "aplikasi-keuangan",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:prepare": "node scripts/prepare-capacitor-web.js",
    "apk:debug": "npm run cap:prepare && npx cap add android && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1"
  }
}
